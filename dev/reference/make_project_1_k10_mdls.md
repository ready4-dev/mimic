# Make project 1 K10 models

make_project_1_k10_mdls() is a Make function that creates a new R
object. Specifically, this function implements an algorithm to make
project 1 k10 models. The function returns K10 (a list).

## Usage

``` r
make_project_1_k10_mdls(X_Ready4useDyad)
```

## Arguments

- X_Ready4useDyad:

  PARAM_DESCRIPTION

## Value

K10 (a list)
