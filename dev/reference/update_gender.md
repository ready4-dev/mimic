# Update gender

update_gender() is an Update function that edits an object, while
preserving core object attributes. Specifically, this function
implements an algorithm to update gender. The function returns Data (a
tibble).

## Usage

``` r
update_gender(data_tb)
```

## Arguments

- data_tb:

  Data (a tibble)

## Value

Data (a tibble)
