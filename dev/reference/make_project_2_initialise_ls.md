# Make project 2 initialise list

make_project_2_initialise_ls() is a Make function that creates a new R
object. Specifically, this function implements an algorithm to make
project 2 initialise list. The function returns Initialise (a list).

## Usage

``` r
make_project_2_initialise_ls(derive_ls = list())
```

## Arguments

- derive_ls:

  Derive (a list), Default: list()

## Value

Initialise (a list)
