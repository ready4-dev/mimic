# MimicEventsList

Data on all events included in model.

## Slots

- `events_ls`:

  Events (a list)

- `last_1L_chr`:

  Last (a character vector of length one)

- `main_chr`:

  Main (a character vector)

- `dissemination_1L_chr`:

  Dissemination (a character vector of length one)
