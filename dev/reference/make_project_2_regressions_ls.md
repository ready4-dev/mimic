# Make project 2 regressions list

make_project_2_regressions_ls() is a Make function that creates a new R
object. Specifically, this function implements an algorithm to make
project 2 regressions list. The function returns Regressions (a list).

## Usage

``` r
make_project_2_regressions_ls()
```

## Value

Regressions (a list)
