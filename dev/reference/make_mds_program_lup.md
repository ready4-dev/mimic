# Make Minimum Dataset program lookup table

make_mds_program_lup() is a Make function that creates a new R object.
Specifically, this function implements an algorithm to make minimum
dataset program lookup table. The function returns Program (a lookup
table).

## Usage

``` r
make_mds_program_lup()
```

## Value

Program (a lookup table)
