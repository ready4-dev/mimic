# Make annual overview

make_annual_overview() is a Make function that creates a new R object.
Specifically, this function implements an algorithm to make annual
overview. The function returns Annual (a tibble).

## Usage

``` r
make_annual_overview(processed_ls)
```

## Arguments

- processed_ls:

  Processed (a list)

## Value

Annual (a tibble)
