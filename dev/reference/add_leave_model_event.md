# Add leave model event

add_leave_model_event() is an Add function that updates an object by
adding new values to new or empty fields. Specifically, this function
implements an algorithm to add leave model event. The function is called
for its side effects and does not return a value.

## Usage

``` r
add_leave_model_event(X_Ready4useDyad)
```

## Arguments

- X_Ready4useDyad:

  PARAM_DESCRIPTION

## Value

X (A dataset and data dictionary pair.)
