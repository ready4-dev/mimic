# Make joiners series

make_joiners_series() is a Make function that creates a new R object.
Specifically, this function implements an algorithm to make joiners
series. The function is called for its side effects and does not return
a value.

## Usage

``` r
make_joiners_series(model_data_ls)
```

## Arguments

- model_data_ls:

  Model data (a list)

## Value

X (A dataset and data dictionary pair.)
