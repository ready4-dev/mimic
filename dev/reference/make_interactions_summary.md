# Make interactions summary

make_interactions_summary() is a Make function that creates a new R
object. Specifically, this function implements an algorithm to make
interactions summary. The function returns Interactions (a tibble).

## Usage

``` r
make_interactions_summary(processed_ls)
```

## Arguments

- processed_ls:

  Processed (a list)

## Value

Interactions (a tibble)
